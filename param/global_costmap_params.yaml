global_costmap:
   global_frame: odom
   robot_base_frame: base_footprint
   update_frequency: 1.0
   publish_frequency: 0.5
   static_map: false
   transform_tolerance: 0.5

   width: 30.0
   height: 30.0
   resolution: 0.1
   transform_tolerance: 0.5

   observation_sources: bump
   bump: {data_type: PointCloud, topic: /pedsim_obstacle_sensor/point_cloud_global, marking: true, clearing: false, min_obstacle_height: 0.0, max_obstacle_height: 1}
